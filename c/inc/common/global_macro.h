#pragma once

#define TOKEN_SIZE_DEF  256
#define FILE_PATH_MAX   512
#define TOKEN_STR_MAX   30

enum {
    CMM_INIT_ARGUMENT_FAILED = -100,// init argument failed
    CMM_GET_INSTANCE_FAILED,        // get instance failed

    CMM_SUCCESS = 0,                // success
};
